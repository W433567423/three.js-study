{
  "name": "three.js-study",
  "version": "1.0.1",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "init": "rm -rf dist && mkdir dist && cp -r public/* dist/",
    "start": "npm run init && parcel index.html",
    "build": "npm run init && parcel build index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gsap": "^3.12.2",
    "parcel": "^2.10.1",
    "three": "^0.157.0"
  },
  "devDependencies": {
    "@types/three": "^0.157.2"
  }
}
